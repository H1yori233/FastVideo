```
git config --local user.name "H1yori233"
git config --local user.email "k1kong@ucsd.edu"
git commit -m "update"
```

```
pre-commit run --all-files
```

num_gpus 必须整除 sp_size、hsdp_replicate_dim、hsdp_shard_dim
hsdp_replicate_dim * hsdp_shard_dim == num_gpus
tp_size 必须能整除模型并行维度

```
python examples/inference/basic/basic_matrixgame_base.py | tee output_inference.log
```

```
bash examples/training/finetune/MatrixGame2.0/preprocess_matrixgame_data_i2v.sh 2>&1 | tee output_preprocess.log
```

```
bash examples/training/finetune/MatrixGame2.0/finetune_i2v.sh 2>&1 | tee output_finetune.log
```