```
git config --local user.name "H1yori233"
git config --local user.email "k1kong@ucsd.edu"
git commit -m "update"
```

```
pre-commit run --all-files
```

num_gpus 必须整除 sp_size、hsdp_replicate_dim、hsdp_shard_dim
hsdp_replicate_dim * hsdp_shard_dim == num_gpus
tp_size 必须能整除模型并行维度

### 收集 ODE pairs

#### 0117_22:00

很容易理解，就是收集

```
    trajectory_latents: Latents at different timesteps
    trajectory_timesteps: Corresponding timesteps
```

```
    noise_pred = current_model(
        latent_model_input,
        ...
    )
    ...
    latents = self.scheduler.step(noise_pred, t,
                latents, **extra_step_kwargs, return_dict=False)[0]
    ...
    trajectory_timesteps.append(t)
    trajectory_latents.append(latents)
```

原文是 `40k` 步

```
bash examples/training/consistency_finetune/causal_matrixgame_ode_init/preprocess_matrixgame_data.sh 2>&1 | tee output_ode.log
```

#### 0118_15:00

不知道为啥 tmux 老是卡死

```
tmux kill-session -t fastvideo_kaiqin
tmux new -s fastvideo_kaiqin
tmux attach -t fastvideo_kaiqin
```

1 trajectory = 1 样本进行 48 次推理记录

```  why 48?
batch.num_inference_steps = 48
```

应该可以只提供 first frame 和 action sequence（还没写）

#### 0119_1830

重新收集 trajectory

```
    @property
    def is_image(self) -> bool:
        """Check if this is an image item."""
        return self.path.endswith(".jpg")
```

发现 preprocess 里一定要 jpg 才行。。。顺带把 validation.json 的都改了

#### 0121_0000
添加了 `override_keyboard_dim_in`，直接输出学生要的 trajectory


### 在 foundation model 上替换 action module

#### 0118_1700

```
python scripts/check_safetensors.py 2>&1 | tee output_check.log
```

```
python scripts/remove_action_module.py 2>&1 | tee output_remove.log
```

### fine-tune causal student model

原文是 `6k` 步

```
bash examples/training/consistency_finetune/causal_matrixgame_ode_init/finetune_ode_init.sh 2>&1 | tee output_finetune_ode.log
```

#### 0119_1300

注意这里是用 causal model，脚本里面有 `--override_transformer_cls_name` 参数
类似的加入了 `--override_action_config`

#### 0119_1500
重写了 ode training pipeline

#### 0120_1400
ode finetuning 跑起来了，但是看起来 checkpoint config 的 override 没 work

#### 0120_2100
修复了 override 后问题在于直接读取会有问题。

先手动重新 init action module，等全部跑通了再修

validation 要关掉，不然会出现

```
ValueError: block_mask was created for block_mask.shape=(1, 1, 2688, 2688) but got q_len=18560 and kv_len=18560.
```

应该是 causal model 的问题？（不确定）之后再修

#### 0122_0030

因为 cache mask 的问题，重写了 causal model 这一部分的代码

#### 0122_1200

ODE init 出来结果都是噪声，很奇怪

### Self-Forcing

原文是 `4k` 步

```
bash examples/distill/MatrixGame2.0/distill_dmd.sh 2>&1 | tee output_sf.log
```

`distillation_pipeline.py` 里面 hardcode 了 `training_args.override_transformer_cls_name = "WanTransformer3DModel"`。。。

#### 0121_1800
原本的 `_build_distill_input_kwargs` 有问题，直接用会导致 

```
RuntimeError: Sizes of tensors must match except in dimension 2. Expected size 3 but got size 21 for tensor number 1 in the list.
```

说明本来只是要 1 个 block 的 `image_latents` 现在给的是全部的，加入 frame range 来切割 `image_latents = image_latents[:, :, frame_start:frame_end, :, :]`

pipeline 有个 bug，会调用两次 `_prepare_dit_inputs`

```
RuntimeError: Given groups=1, weight of size [1536, 36, 1, 2, 2], expected input[1, 40, 3, 44, 80] to have 36 channels, but got 40 channels instead
```

没找出原因在哪，先根据 shape 强制 return

#### 0121_2200
action_module 使用普通的 nn.Linear，这好像在 FSDP 环境下不支持

```
RuntimeError: aten.addmm.default: got mixed torch.Tensor and DTensor, need to convert all torch.Tensor to DTensor before calling distributed operators!
```

替换成 MLP，此外 matrixgame.py 的也加了参数映射

The learning rate is 6e-6. The chunk
size of latent frames and attention local size are set as 3 and 6, respectively.
