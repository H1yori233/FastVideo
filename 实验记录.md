```
git config --local user.name "H1yori233"
git config --local user.email "k1kong@ucsd.edu"
git commit -m "update"
```

```
pre-commit run --all-files
```

num_gpus 必须整除 sp_size、hsdp_replicate_dim、hsdp_shard_dim
hsdp_replicate_dim * hsdp_shard_dim == num_gpus
tp_size 必须能整除模型并行维度

不知道为啥 tmux 老是卡死

```
tmux kill-session -t fastvideo_kaiqin
tmux new -s fastvideo_kaiqin
tmux attach -t fastvideo_kaiqin
```


## Finetune

```
python examples/inference/basic/basic_matrixgame_base.py | tee output_inference.log
```

```
bash examples/training/finetune/MatrixGame2.0/preprocess_matrixgame_data_i2v.sh 2>&1 | tee output_preprocess.log
```

```
bash examples/training/finetune/MatrixGame2.0/finetune_i2v.sh 2>&1 | tee output_finetune.log
```

6 gpus, bs 12, steps 100 大概半小时

## SF

```
bash examples/training/consistency_finetune/causal_matrixgame_ode_init/preprocess_matrixgame_data.sh 2>&1 | tee output_ode_preprocess.log
```

```
python visualize_trajectory.py   --parquet_path "mc_wasd_action/preprocessed/combined_parquet_dataset/worker_0/data_chunk_0.parquet"   --model_path "Matrix-Game-2.0-Foundation-Diffusers"   --output_dir "mc_wasd_action/visualizations" --decode_steps all
```

32 个 trajectory，18 分钟

```
bash examples/training/consistency_finetune/causal_matrixgame_ode_init/finetune_ode_init.sh 2>&1 | tee output_ode_init.log
```

---

```
python scripts/huggingface/download_hf.py --repo_id alexzms/FastvideoWorldModel-MC --repo_type dataset --local_dir ./FastvideoWorldModel-MC
```

```
python scripts/huggingface/download_hf.py --repo_id H1yori233/Matrix-Game-2.0-Foundation-MC32k-Diffusers --repo_type model --local_dir ../Matrix-Game-2.0-Foundation-MC32k-Diffusers
```

```
python examples/training/finetune/MatrixGame2.0/process.py FastvideoWorldModel-MC/data
```
```
examples/training/finetune/MatrixGame2.0/validate.py mc_32k
```

```
bash examples/training/finetune/MatrixGame2.0/8gpu.sh 2>&1 | tee output_preprocess.log
```

---

## M2

```
conda activate alexfv
```

```
bash examples/training/consistency_finetune/causal_matrixgame_ode_init/launch_matrixgame_ode_slurm.sh
```

```
sbatch examples/training/consistency_finetune/causal_matrixgame_ode_init/finetune_ode_init_64gpu.slurm
```